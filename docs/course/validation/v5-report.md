# v5.0 实际验证报告

本报告区分教材静态检查、机制实验、导入工具与用户应用。检查日期：2026-09-20。

## 已执行

- 教材结构与链接检查：34课、204个核心文档、218个知识点目的桥接；静态检查通过。链接与Python文件数量以随附最终JSON为准。
- 原教材工具回归：47项通过，无skip。
- 原31个Python概念示例：全部实际执行，退出0。此结果只说明示例运行，不证明完整应用。
- 新增13个机制实验：全部实际执行，断言通过，JSON verified=true。
- J01/J02两个Java概念示例：OpenJDK21编译并运行通过。
- 导入工具：21项离线临时项目回归通过，包括预览、哈希冲突、备份、恢复、后续编辑保护、路径约束、符号链接、清单篡改和Windows LF/CRLF兼容检查。

## 未执行或不能据此声称通过

- 用户仓库devagent应用全量测试：本次没有复制/执行用户应用；原有42项测试状态仍是固定提交的历史交接记录，不当作本版实测。
- 8个LangChain/LangGraph相关框架示例：当前环境缺少相关依赖，保持blocked。保留源码并核对官方接口资料，不声称运行成功。
- J03 PostgreSQL脚本：未提供受控数据库实例，未运行；不能以SQLite概念实验替代。
- 真实模型、embedding、付费API、Java跨服务和数据库联调：未运行。
- 用户Windows真实符号链接验证：原交接记录中的WinError1314仍未解决。本版导入器在Linux临时目录的symlink测试不是那个应用验收。
- 并发文件系统强隔离、容器安全、恰好一次副作用、生产检索质量：不是本报告证明的范围。

## 环境与证据

本次Python3.13.5、Linux；Java为OpenJDK21.0.11。用户课程基线与Codex记录中的Python3.12/3.14、Windows环境分开记录，不混称同一机器。

示例逐条命令对象、退出码、stdout/stderr和未运行理由见[v5示例运行记录](v5-example-results.json)。这不是学生成绩单，也不是由第三方完成的独立审核。

## 阅读器检查

Chromium通过自包含HTML内容加载，实际检查了34课、204页签、全部嵌入文档、搜索、答案展开、复制反馈、手机目录与390px页面布局；没有JavaScript异常。旧L02文档哈希 `d33595c5a4951` 保留。

当前容器浏览器的管理员策略阻止file://导航，因此没有把直接双击文件打开列为已实测。HTML不依赖外部脚本/样式/字体；内容加载与交互测试通过。使用者可在普通浏览器打开；若所在环境禁止本地HTML，Markdown仍可阅读，不要求降低浏览器安全设置。

未展开答案不参与搜索，答案门槛只帮助学习节奏，不是安全或保密机制。详见[浏览器检查记录](v5-browser.json)。

## 导入演练的验证范围

另在临时构造的Git项目标记目录中，对完整v4.2教材执行：dry-run → apply → 二次apply → rollback；检查替换/新增数量、内容哈希、备份与恢复，应用文件和个人记录必须保持不变。最终结果见[完整导入演练记录](v5-full-import.json)。

该演练不是在用户真实工作区运行，不证明用户当前不存在文档冲突；Codex仍须先预览和比对。导入器不是并发文件系统沙箱，执行期间不要让其他程序同时修改教材。文件级替换是原子的，但整套教材不是单事务；中途中断按日志和备份恢复。
