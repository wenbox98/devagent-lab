# L09｜复习与迁移答案：先尝试，再打开

> v5阅读说明：先保留你在03/05中的首次判断再核对。本文件保留原课参考思路，不代表当前主项目已全部实现，也不代表学习者已掌握。

## v5核对时的三条约束

本课基础方案以[新版教程](01-教程.md)第1/3节和[02合同](02-交给AI.md)为准；原参考中的建议文件/符号需要映射真实代码。不要以修改参考标准迎合错误实现。

验证必须说明输入、第一处关键判断、实际副作用和结果。模拟、框架离线、真实服务、跨进程持久性和个人能力分开报告。

如果参考中的扩展条件超出现有代码，写待实现/未验证，不能把目标当当前事实。L00—L02具体差距见[当前基线](../../19-当前代码基线与学习接续.md)。

---

## 原课参考内容（保留作对照）

[课程目录](../../01-课程导航.md) · [上一课 L08](../L08/01-教程.md) · [下一课 L10](../L10/01-教程.md)

这份给学习者核对，**不需要交给代码AI生成基础实现**。参考解法不是唯一解，关键是约束、证据与边界。

## 先确认你核对的是哪个对象

本文件的“应该”是目标合同，不是对未生成项目的实测报告。概念题对象是`docs/course/lessons/L09/00-example.py`（练习改动在`.local/concepts/L09/00-example.py`）；项目题对象是`docs/learning-records/L09-code-map.json`中的实际源码。不能把这里的目标答案反填成你的实际观察。

对应题目和执行步骤：[03-实验与排错.md](03-实验与排错.md)；定位规则：[05-过关卡与代码地图.md](05-过关卡与代码地图.md)。

## 闭卷回忆参考

### 1. exit code=0是否充分？

不，需有效测试数、目标断言、回归和约束检查。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 2. 为什么记录每次补丁版本？

避免报告对应旧代码，便于比较和恢复。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 3. 删掉失败测试可以提高成功率吗？

会破坏验收独立性，属于无效成功。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

## 原教程独立练习

题目：增加page_no=0的验收，观察只修偏移的方案为什么仍不够。

提示：先写期望行为：非法页码抛明确错误，不能让Python负索引悄悄给结果。

参考：修复需同时满足指定边界与旧行为；新增限制必须与任务约定一致，不能无依据改公共语义。

## 近迁移参考与验收

题目：加入依赖缺失场景并禁止反复改业务文件。

参考：分类environment_error，补丁次数不增加，报告精确缺失项。

先写失败输入与预期，再实现最小变化；保留一次失败与修复后的输出。检查通过不代表覆盖所有情况，至少解释一个尚未处理的边界。

## 远迁移参考与验收

题目：迁移到数据清洗Agent：脚本运行结束但数据质量下降。

参考：把运行完成与质量验收拆开；校验行数、约束、异常比例与不可变原始数据。

参考组织方式：复用本课决策方法，逐项列出新业务的身份、数据、副作用和结果来源；明确哪些旧假设已不成立。不要只替换类名。允许有不同实现，但必须给出能让错误方案失败的验收。

## 自评与下一步

每题按本文件“只说术语0分、解释机制1分、给出具体分支与反例2分”的标准；远迁移按4项各1分：条件明确、方案有比较、验证可执行、限制诚实。至少3/4且无硬边界错误为本轮通过。这里的阈值是课程训练门槛，不是行业招聘标准。

- 只说术语：回到01相应知识点，重走一次案例。
- 机制懂但无法编码：补本课语言连接项，独立写一个最小函数。
- 能完成原题但换条件失败：再做一个近迁移变体，记录失效的假设。
- 能讲方案却没有证据：按03重做故障注入并保存实际输出。

AI可以在你提交答案后评审，但不能替你给“已掌握”盖章。D7/D21检查用于发现遗忘，再决定补练。


## 代码地图预测题：目标契约与反事实

**题目：** pytest退出5与退出1都非零，能否都解释成补丁没修好？

**参考判断：** 不能。一个常见含义是没有收集到测试，另一个是测试失败；实际以所用测试器官方退出码为准。先区分环境/收集/断言/超时，才知道应该修代码还是修运行配置。

**反事实核对：** 分别制造导入失败、0测试和真实断言失败；检查下一步动作不同，不统一重试模型。

**远迁移方向：** 数据管道出错：schema错误、输入空集和质量断言失败如何分层？

评分：只说名词0分；能说机制1分；能预测具体分支且给出反例2分。还必须有独立修改和失败证据；这个分数不是就业预测。允许有不同实现，但不得跳过安全/事实边界。

## 从实验页移入的独立变体参考

最终成功需要目标、回归、作用范围等约束共同成立。回归失败必须阻止成功判定；是否回滚由工作区策略决定，但不能隐藏失败。
