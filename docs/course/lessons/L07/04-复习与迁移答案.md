# L07｜复习与迁移答案：先尝试，再打开

[课程目录](../../01-课程导航.md) · [上一课 L06](../L06/01-教程.md) · [下一课 L08](../L08/01-教程.md)

这份给学习者核对，**不需要交给代码AI生成基础实现**。参考解法不是唯一解，关键是约束、证据与边界。

## 先确认你核对的是哪个对象

本文件的“应该”是目标合同，不是对未生成项目的实测报告。概念题对象是`docs/course/lessons/L07/00-example.py`（练习改动在`.local/concepts/L07/00-example.py`）；项目题对象是`docs/learning-records/L07-code-map.json`中的实际源码。不能把这里的目标答案反填成你的实际观察。

对应题目和执行步骤：[03-实验与排错.md](03-实验与排错.md)；定位规则：[05-过关卡与代码地图.md](05-过关卡与代码地图.md)。

## 闭卷回忆参考

### 1. 为什么写入前要再核对？

读取之后文件可能变化，模型生成过程不是锁。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 2. rename能保证多文件事务吗？

不能，原子边界通常只涵盖单次文件替换。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 3. 回滚为何也需要前置条件？

备份之后可能有新改动，盲目恢复会再次覆盖。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

## 原教程独立练习

题目：让删除文件也需要expected_hash，并说明删除后如何处理不存在与重复请求。

提示：删除同样是有副作用操作，不能只关注新增和修改。

参考：先区分目标本来不存在和本操作已删除；要实现跨重试识别还需L10操作记录，不能只凭缺文件判断成功。

## 近迁移参考与验收

题目：加入只读文件保护。

参考：即使哈希正确也拒绝写；先验证权限，再检查版本，拒绝后字节不变。

先写失败输入与预期，再实现最小变化；保留一次失败与修复后的输出。检查通过不代表覆盖所有情况，至少解释一个尚未处理的边界。

## 远迁移参考与验收

题目：迁移到后台编辑配置：两名管理员同时保存。

参考：用版本号检测过期更新，冲突时展示差异；不要让最后写入静默覆盖。

参考组织方式：复用本课决策方法，逐项列出新业务的身份、数据、副作用和结果来源；明确哪些旧假设已不成立。不要只替换类名。允许有不同实现，但必须给出能让错误方案失败的验收。

## 自评与下一步

每题按本文件“只说术语0分、解释机制1分、给出具体分支与反例2分”的标准；远迁移按4项各1分：条件明确、方案有比较、验证可执行、限制诚实。至少3/4且无硬边界错误为本轮通过。这里的阈值是课程训练门槛，不是行业招聘标准。

- 只说术语：回到01相应知识点，重走一次案例。
- 机制懂但无法编码：补本课语言连接项，独立写一个最小函数。
- 能完成原题但换条件失败：再做一个近迁移变体，记录失效的假设。
- 能讲方案却没有证据：按03重做故障注入并保存实际输出。

AI可以在你提交答案后评审，但不能替你给“已掌握”盖章。D7/D21检查用于发现遗忘，再决定补练。


## 代码地图预测题：目标契约与反事实

**题目：** 审批后文件被他人改动，即使补丁还能匹配，也应该照常写吗？

**参考判断：** 不能默认允许；审批和补丁都应绑定基线。报告conflict并重新读取，防止旧意图覆盖新状态。比较与写入的原子性/锁策略要与单写假设一致。

**反事实核对：** 在读和写之间插入一次受控变更，断言不覆盖新内容。不要以模糊匹配成功代替版本校验。

**远迁移方向：** Java更新库存时，版本条件失效后为什么不能无条件再update？

评分：只说名词0分；能说机制1分；能预测具体分支且给出反例2分。还必须有独立修改和失败证据；这个分数不是就业预测。允许有不同实现，但不得跳过安全/事实边界。

## 从实验页移入的独立变体参考

必须检查目标当前指纹与expected_hash；冲突不等于可直接强制覆盖。测试应断言原新增内容未丢失，不只断言返回conflict。重新生成补丁需要重新核对语义。
