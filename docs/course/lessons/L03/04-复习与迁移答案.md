# L03｜复习与迁移答案：先尝试，再打开

> v5阅读说明：先保留你在03/05中的首次判断再核对。本文件保留原课参考思路，不代表当前主项目已全部实现，也不代表学习者已掌握。

## v5核对时的三条约束

本课基础方案以[新版教程](01-教程.md)第1/3节和[02合同](02-交给AI.md)为准；原参考中的建议文件/符号需要映射真实代码。不要以修改参考标准迎合错误实现。

验证必须说明输入、第一处关键判断、实际副作用和结果。模拟、框架离线、真实服务、跨进程持久性和个人能力分开报告。

如果参考中的扩展条件超出现有代码，写待实现/未验证，不能把目标当当前事实。L00—L02具体差距见[当前基线](../../19-当前代码基线与学习接续.md)。

---

## 原课参考内容（保留作对照）

[课程目录](../../01-课程导航.md) · [上一课 L02](../L02/01-教程.md) · [下一课 L04](../L04/01-教程.md)

这份给学习者核对，**不需要交给代码AI生成基础实现**。参考解法不是唯一解，关键是约束、证据与边界。

## 先确认你核对的是哪个对象

本文件的“应该”是目标合同，不是对未生成项目的实测报告。概念题对象是`docs/course/lessons/L03/00-example.py`（练习改动在`.local/concepts/L03/00-example.py`）；项目题对象是`docs/learning-records/L03-code-map.json`中的实际源码。不能把这里的目标答案反填成你的实际观察。

对应题目和执行步骤：[03-实验与排错.md](03-实验与排错.md)；定位规则：[05-过关卡与代码地图.md](05-过关卡与代码地图.md)。

## 闭卷回忆参考

### 1. 为什么不能eval模型生成的工具名？

会把数据提升为执行代码；应用只允许注册表中的函数。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 2. 描述改动需要测试吗？

需要，它会影响模型路由；不仅是文案。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 3. 结果顺序改变怎么办？

通过tool_call_id关联，而不是位置。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

## 原教程独立练习

题目：增加一个只返回受控仓库文件数量的工具，保留统一协议。

提示：从Spec、处理器、注册、正常与异常测试四个位置追踪，不为新工具新建另一套循环。

参考：复用执行器，加入有限能力；返回计数必须实际统计允许范围，不能在demo里写死。

## 近迁移参考与验收

题目：加入未知字段拒绝或兼容策略，二选一说明理由。

参考：Schema行为与实际执行一致；未知工具和越界路径仍被拒绝。

先写失败输入与预期，再实现最小变化；保留一次失败与修复后的输出。检查通过不代表覆盖所有情况，至少解释一个尚未处理的边界。

## 远迁移参考与验收

题目：给报销系统设计“查额度”和“提交报销”两个工具。

参考：区分只读与副作用，金额校验与身份授权分离，提交需要操作ID及状态查询。

参考组织方式：复用本课决策方法，逐项列出新业务的身份、数据、副作用和结果来源；明确哪些旧假设已不成立。不要只替换类名。允许有不同实现，但必须给出能让错误方案失败的验收。

## 自评与下一步

每题按本文件“只说术语0分、解释机制1分、给出具体分支与反例2分”的标准；远迁移按4项各1分：条件明确、方案有比较、验证可执行、限制诚实。至少3/4且无硬边界错误为本轮通过。这里的阈值是课程训练门槛，不是行业招聘标准。

- 只说术语：回到01相应知识点，重走一次案例。
- 机制懂但无法编码：补本课语言连接项，独立写一个最小函数。
- 能完成原题但换条件失败：再做一个近迁移变体，记录失效的假设。
- 能讲方案却没有证据：按03重做故障注入并保存实际输出。

AI可以在你提交答案后评审，但不能替你给“已掌握”盖章。D7/D21检查用于发现遗忘，再决定补练。


## 代码地图预测题：目标契约与反事实

**题目：** 模型传入tenant_id=admin，Schema完全合法，应该执行吗？

**参考判断：** 不应该。合法JSON只证明形状正确；身份由可信宿主上下文决定。隐藏工具也不能替代执行时授权，因为模型仍可请求不存在或不可用的工具。

**反事实核对：** 参数多一个未知字段、整数位置传true、call_id重复时分别观察校验和副作用计数。

**远迁移方向：** 把工具看成RPC：参数合法、调用者有权和操作成功是三个独立判断。

评分：只说名词0分；能说机制1分；能预测具体分支且给出反例2分。还必须有独立修改和失败证据；这个分数不是就业预测。允许有不同实现，但不得跳过安全/事实边界。

## 从实验页移入的独立变体参考

结果必须按调用ID匹配，参数和返回应可追踪。错配应被协议层拒绝或标记，不继续把错误上下文交给模型。工具业务单测可能仍通过，因此还要协议测试。
