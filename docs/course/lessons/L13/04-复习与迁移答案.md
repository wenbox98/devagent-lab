# L13｜复习与迁移答案：先尝试，再打开

> v5阅读说明：先保留你在03/05中的首次判断再核对。本文件保留原课参考思路，不代表当前主项目已全部实现，也不代表学习者已掌握。

## v5核对时的三条约束

本课基础方案以[新版教程](01-教程.md)第1/3节和[02合同](02-交给AI.md)为准；原参考中的建议文件/符号需要映射真实代码。不要以修改参考标准迎合错误实现。

验证必须说明输入、第一处关键判断、实际副作用和结果。模拟、框架离线、真实服务、跨进程持久性和个人能力分开报告。

如果参考中的扩展条件超出现有代码，写待实现/未验证，不能把目标当当前事实。L00—L02具体差距见[当前基线](../../19-当前代码基线与学习接续.md)。

---

## 原课参考内容（保留作对照）

[课程目录](../../01-课程导航.md) · [上一课 F02](../F02/01-教程.md) · [下一课 L14](../L14/01-教程.md)

这份给学习者核对，**不需要交给代码AI生成基础实现**。参考解法不是唯一解，关键是约束、证据与边界。

## 先确认你核对的是哪个对象

本文件的“应该”是目标合同，不是对未生成项目的实测报告。概念题对象是`docs/course/lessons/L13/00-example.py`（练习改动在`.local/concepts/L13/00-example.py`）；项目题对象是`docs/learning-records/L13-code-map.json`中的实际源码。不能把这里的目标答案反填成你的实际观察。

对应题目和执行步骤：[03-实验与排错.md](03-实验与排错.md)；定位规则：[05-过关卡与代码地图.md](05-过关卡与代码地图.md)。

## 闭卷回忆参考

### 1. 两工具都能完成时，标准答案必须唯一吗？

不应唯一；记录可接受集合，以最终任务成功及约束验收为准。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 2. 总体成功率高4个百分点就该发布吗？

不一定；先看各组门槛、样本波动、成本和违规。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 3. 优化提示词前先看什么？

可观测任务条件、候选资格、真实调用轨迹和失败类别。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 4. 自动回退会不会有害？

会；可能越权、语义不兼容、重复副作用或增加成本。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

## 原教程独立练习

题目：把A/B合并为一个入口的设计写下来，列出模型少了什么决策、程序增加了什么责任。

提示：合并隐藏的是实现选择，不是权限、版本和观测需求。

参考：统一入口减少相似工具歧义；内部要获取上下文、路由、校验与记录。若丢失能力表达或调试困难，拆分可能更合适。

## 近迁移参考与验收

题目：加入“两个工具都合格且都正确”的20个任务样本，修改评分器。

参考：不得按唯一工具标签误罚；记录最终成功、额外调用与成本，写出发布条件。

先写失败输入与预期，再实现最小变化；保留一次失败与修复后的输出。检查通过不代表覆盖所有情况，至少解释一个尚未处理的边界。

## 远迁移参考与验收

题目：工单Agent有规则分类器A与LLM分类器B，VIP与普通用户表现不同，如何优化？

参考：把人群还原成任务难度、SLA和权限；设计分组基线、硬过滤、路由与回退，不只给VIP固定选贵模型。

参考组织方式：复用本课决策方法，逐项列出新业务的身份、数据、副作用和结果来源；明确哪些旧假设已不成立。不要只替换类名。允许有不同实现，但必须给出能让错误方案失败的验收。

## 自评与下一步

每题按本文件“只说术语0分、解释机制1分、给出具体分支与反例2分”的标准；远迁移按4项各1分：条件明确、方案有比较、验证可执行、限制诚实。至少3/4且无硬边界错误为本轮通过。这里的阈值是课程训练门槛，不是行业招聘标准。

- 只说术语：回到01相应知识点，重走一次案例。
- 机制懂但无法编码：补本课语言连接项，独立写一个最小函数。
- 能完成原题但换条件失败：再做一个近迁移变体，记录失效的假设。
- 能讲方案却没有证据：按03重做故障注入并保存实际输出。

AI可以在你提交答案后评审，但不能替你给“已掌握”盖章。D7/D21检查用于发现遗忘，再决定补练。


## 代码地图预测题：目标契约与反事实

**题目：** 便宜工具不支持某文件类型，是否应先试一下以节约成本？

**参考判断：** 已知不满足硬条件时应先排除。只在可行候选中优化价格/延迟。回退也消耗预算且可能有副作用，不能无限尝试。

**反事实核对：** 按任务组比较总成功率和每成功任务成本；平均值不能掩盖特定群体退化。

**远迁移方向：** 快模型与慢模型路由：硬SLA和低价格哪个先处理？

评分：只说名词0分；能说机制1分；能预测具体分支且给出反例2分。还必须有独立修改和失败证据；这个分数不是就业预测。允许有不同实现，但不得跳过安全/事实边界。

## 从实验页移入的独立变体参考

索引版本属于执行前需核对的条件；过期后是否退回A取决于能力、权限及剩余预算，不能无条件fallback。评分采用acceptable_tools集合，同时看最终任务成功。
