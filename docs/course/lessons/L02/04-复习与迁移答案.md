# L02｜复习与迁移答案：先尝试，再打开

> v5阅读说明：先保留你在03/05中的首次判断再核对。本文件保留原课参考思路，不代表当前主项目已全部实现，也不代表学习者已掌握。

## v5核对时的三条约束

本课基础方案以[新版教程](01-教程.md)第1/3节和[02合同](02-交给AI.md)为准；原参考中的建议文件/符号需要映射真实代码。不要以修改参考标准迎合错误实现。

验证必须说明输入、第一处关键判断、实际副作用和结果。模拟、框架离线、真实服务、跨进程持久性和个人能力分开报告。

如果参考中的扩展条件超出现有代码，写待实现/未验证，不能把目标当当前事实。L00—L02具体差距见[当前基线](../../19-当前代码基线与学习接续.md)。

---

## 原课参考内容（保留作对照）

[课程目录](../../01-课程导航.md) · [上一课 L01](../L01/01-教程.md) · [下一课 L03](../L03/01-教程.md)

这份给学习者核对，**不需要交给代码AI生成基础实现**。参考解法不是唯一解，关键是约束、证据与边界。

## 先确认你核对的是哪个对象

本文件的“应该”是目标合同，不是对未生成项目的实测报告。概念题对象是`docs/course/lessons/L02/00-example.py`（练习改动在`.local/concepts/L02/00-example.py`）；项目题对象是`docs/learning-records/L02-code-map.json`中的实际源码。不能把这里的目标答案反填成你的实际观察。

对应题目和执行步骤：[03-实验与排错.md](03-实验与排错.md)；定位规则：[05-过关卡与代码地图.md](05-过关卡与代码地图.md)。

## 闭卷回忆参考

### 1. 哈希相同能证明有权限吗？

不能，哈希证明内容一致性，不证明访问资格。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 2. 为什么只返回内容不够？

缺少位置与版本，无法核对和安全修改。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 3. resolve能消灭全部风险吗？

不能，仍需考虑检查使用竞态和隔离边界。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

## 原教程独立练习

题目：增加只允许读取指定文本后缀的规则，并解释它能控制什么、不能控制什么。

提示：后缀规则是可用性策略，不等于文件内容可信，也不能替代路径边界。

参考：后缀检查放在已解析路径和文件类型校验附近；符号链接目标仍需检查，允许.py不意味着允许执行。

## 近迁移参考与验收

题目：给read_file添加最大行数，输出next_start。

参考：超限有明确截断标记，边界页不重复、不漏行，根外访问仍拒绝。

先写失败输入与预期，再实现最小变化；保留一次失败与修复后的输出。检查通过不代表覆盖所有情况，至少解释一个尚未处理的边界。

## 远迁移参考与验收

题目：迁移到对象存储下载：用户传bucket/key而非路径。

参考：从身份映射允许对象范围；不要信用户提供的租户前缀，分别处理授权、大小与版本。

参考组织方式：复用本课决策方法，逐项列出新业务的身份、数据、副作用和结果来源；明确哪些旧假设已不成立。不要只替换类名。允许有不同实现，但必须给出能让错误方案失败的验收。

## 自评与下一步

每题按本文件“只说术语0分、解释机制1分、给出具体分支与反例2分”的标准；远迁移按4项各1分：条件明确、方案有比较、验证可执行、限制诚实。至少3/4且无硬边界错误为本轮通过。这里的阈值是课程训练门槛，不是行业招聘标准。

- 只说术语：回到01相应知识点，重走一次案例。
- 机制懂但无法编码：补本课语言连接项，独立写一个最小函数。
- 能完成原题但换条件失败：再做一个近迁移变体，记录失效的假设。
- 能讲方案却没有证据：按03重做故障注入并保存实际输出。

AI可以在你提交答案后评审，但不能替你给“已掌握”盖章。D7/D21检查用于发现遗忘，再决定补练。


## 代码地图预测题：目标契约与反事实

**题目：** 根为/tmp/repo，/tmp/repo2/a.py是否允许？软链接指向外部呢？

**参考判断：** 字符串前缀相同不代表祖先关系；解析软链接后再做路径归属检查。检查与打开之间仍有竞态，教学实现限定可信单写工作区，不能声称阻挡任意恶意并发替换。

**反事实核对：** 给出兄弟目录、../、绝对路径、软链接逃逸四个反例；不能只测正常路径。

**远迁移方向：** Java下载接口中的文件ID能否直接当文件路径？设计“ID映射→授权→读取”的边界。

评分：只说名词0分；能说机制1分；能预测具体分支且给出反例2分。还必须有独立修改和失败证据；这个分数不是就业预测。允许有不同实现，但不得跳过安全/事实边界。

## 从实验页移入的独立变体参考

兄弟目录和目录外链接拒绝，合法子文件允许；不要以startswith判断。跳过的平台能力不等于通过，Windows下符号链接权限差异应留证。
