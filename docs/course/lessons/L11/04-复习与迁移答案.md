# L11｜复习与迁移答案：先尝试，再打开

[课程目录](../../01-课程导航.md) · [上一课 L10](../L10/01-教程.md) · [下一课 L12](../L12/01-教程.md)

这份给学习者核对，**不需要交给代码AI生成基础实现**。参考解法不是唯一解，关键是约束、证据与边界。

## 先确认你核对的是哪个对象

本文件的“应该”是目标合同，不是对未生成项目的实测报告。概念题对象是`docs/course/lessons/L11/00-example.py`（练习改动在`.local/concepts/L11/00-example.py`）；项目题对象是`docs/learning-records/L11-code-map.json`中的实际源码。不能把这里的目标答案反填成你的实际观察。

对应题目和执行步骤：[03-实验与排错.md](03-实验与排错.md)；定位规则：[05-过关卡与代码地图.md](05-过关卡与代码地图.md)。

## 闭卷回忆参考

### 1. 重复调用必定无进展吗？

不，先看代码和环境状态是否变化。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 2. 最大步数能替代费用预算吗？

不能，每步消耗差异很大。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 3. 停止时必须失败吗？

要准确区分受阻、预算耗尽和已完成，不伪造成功。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

## 原教程独立练习

题目：构造三次同名read但每次内容不同的任务，检查误停。

提示：动作相同不代表观察相同；两者都要看。

参考：记录不同文件/版本和新增证据，回归真正死循环仍能被阻止，不能为避免误停彻底关闭限制。

## 近迁移参考与验收

题目：加入允许用户补充信息后重置重复计数的规则。

参考：只在有效新信息进入状态时重置，改一句总结不能重置。

先写失败输入与预期，再实现最小变化；保留一次失败与修复后的输出。检查通过不代表覆盖所有情况，至少解释一个尚未处理的边界。

## 远迁移参考与验收

题目：迁移到重试队列：同任务连续失败十次。

参考：依据错误类别与状态变化决定暂停/死信/人工处理，不无限回队列。

参考组织方式：复用本课决策方法，逐项列出新业务的身份、数据、副作用和结果来源；明确哪些旧假设已不成立。不要只替换类名。允许有不同实现，但必须给出能让错误方案失败的验收。

## 自评与下一步

每题按本文件“只说术语0分、解释机制1分、给出具体分支与反例2分”的标准；远迁移按4项各1分：条件明确、方案有比较、验证可执行、限制诚实。至少3/4且无硬边界错误为本轮通过。这里的阈值是课程训练门槛，不是行业招聘标准。

- 只说术语：回到01相应知识点，重走一次案例。
- 机制懂但无法编码：补本课语言连接项，独立写一个最小函数。
- 能完成原题但换条件失败：再做一个近迁移变体，记录失效的假设。
- 能讲方案却没有证据：按03重做故障注入并保存实际输出。

AI可以在你提交答案后评审，但不能替你给“已掌握”盖章。D7/D21检查用于发现遗忘，再决定补练。


## 代码地图预测题：目标契约与反事实

**题目：** 两次错误文本不同，但修改文件和失败测试完全相同，算进展吗？

**参考判断：** 不能仅按文案判断。进展应来自任务相关可观察事实，如新证据、变化的版本、缩小的失败集合。预算上限负责兜底，不是进展判断本身。

**反事实核对：** 改变模型措辞却保持工具参数和文件hash不变，检测重复循环。

**远迁移方向：** 网页研究Agent反复改写摘要，但没有新来源，如何检测？

评分：只说名词0分；能说机制1分；能预测具体分支且给出反例2分。还必须有独立修改和失败证据；这个分数不是就业预测。允许有不同实现，但不得跳过安全/事实边界。

## 从实验页移入的独立变体参考

指纹需要包含任务相关状态而非自然语言回答；B的新版本属于新证据。给出明确阈值与误判条件，不把启发式包装成完美判定。
