# J03｜复习与迁移答案：先尝试，再打开

[课程目录](../../01-课程导航.md) · [上一课 J02](../J02/01-教程.md) · [下一课 L21](../L21/01-教程.md)

这份给学习者核对，**不需要交给代码AI生成基础实现**。参考解法不是唯一解，关键是约束、证据与边界。

## 先确认你核对的是哪个对象

本文件的“应该”是目标合同，不是对未生成项目的实测报告。概念题对象是`docs/course/lessons/J03/00-example.sql`（练习改动在`.local/concepts/J03/00-example.sql`）；项目题对象是`docs/learning-records/J03-code-map.json`中的实际源码。不能把这里的目标答案反填成你的实际观察。

对应题目和执行步骤：[03-实验与排错.md](03-实验与排错.md)；定位规则：[05-过关卡与代码地图.md](05-过关卡与代码地图.md)。

## 闭卷回忆参考

### 1. Index Scan一定快吗？

不，取决于数据分布、返回比例和随机访问成本。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 2. 为什么不随手ANALYZE写SQL？

它会执行语句，应只在受控测试环境并明确事务处理。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 3. 连接池满一定加连接吗？

先看长事务、泄漏、下游等待与数据库承载能力。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

## 原教程独立练习

题目：给一个“总耗时高但SQL执行很短”的案例写排查顺序。

提示：查看拿连接、线程排队、下游HTTP和锁等待。

参考：先按阶段计时定位最大部分，再用线程与池指标区分；不要因为系统用了数据库就先加索引。

## 近迁移参考与验收

题目：加入分页稳定次级键id并比较深分页。

参考：排序无重复/漏项的规则明确；记录数据变更时的边界，不声称所有并发下绝对快照一致。

先写失败输入与预期，再实现最小变化；保留一次失败与修复后的输出。检查通过不代表覆盖所有情况，至少解释一个尚未处理的边界。

## 远迁移参考与验收

题目：迁移到向量检索服务：API慢但向量查询本身不慢。

参考：同样拆连接、过滤、网络、重排与序列化，先定位关键路径再换数据库。

参考组织方式：复用本课决策方法，逐项列出新业务的身份、数据、副作用和结果来源；明确哪些旧假设已不成立。不要只替换类名。允许有不同实现，但必须给出能让错误方案失败的验收。

## 自评与下一步

每题按本文件“只说术语0分、解释机制1分、给出具体分支与反例2分”的标准；远迁移按4项各1分：条件明确、方案有比较、验证可执行、限制诚实。至少3/4且无硬边界错误为本轮通过。这里的阈值是课程训练门槛，不是行业招聘标准。

- 只说术语：回到01相应知识点，重走一次案例。
- 机制懂但无法编码：补本课语言连接项，独立写一个最小函数。
- 能完成原题但换条件失败：再做一个近迁移变体，记录失效的假设。
- 能讲方案却没有证据：按03重做故障注入并保存实际输出。

AI可以在你提交答案后评审，但不能替你给“已掌握”盖章。D7/D21检查用于发现遗忘，再决定补练。


## 代码地图预测题：目标契约与反事实

**题目：** SQL慢一定是缺索引吗？EXPLAIN ANALYZE能随便用于写语句吗？

**参考判断：** 不一定，还可能等待连接、锁、网络或返回过量数据。ANALYZE会实际执行，写语句须在受控可回滚条件下谨慎操作。先区分等待和执行时间。

**反事实核对：** 对固定教学数据记录计划、实际行数和耗时；再改索引或查询，不同时改所有配置。

**远迁移方向：** Agent检索慢：是embedding、向量查询还是重排？用同一分段方法排障。

评分：只说名词0分；能说机制1分；能预测具体分支且给出反例2分。还必须有独立修改和失败证据；这个分数不是就业预测。允许有不同实现，但不得跳过安全/事实边界。

## 从实验页移入的独立变体参考

检查实际过滤行数、返回比例、统计信息、表规模与排序成本；不能强迫优化器使用索引来制造改善。没有PostgreSQL环境就标blocked，不能用模拟计划声称实测。
