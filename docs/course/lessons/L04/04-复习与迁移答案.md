# L04｜复习与迁移答案：先尝试，再打开

[课程目录](../../01-课程导航.md) · [上一课 L03](../L03/01-教程.md) · [下一课 L05](../L05/01-教程.md)

这份给学习者核对，**不需要交给代码AI生成基础实现**。参考解法不是唯一解，关键是约束、证据与边界。

## 先确认你核对的是哪个对象

本文件的“应该”是目标合同，不是对未生成项目的实测报告。概念题对象是`docs/course/lessons/L04/00-example.py`（练习改动在`.local/concepts/L04/00-example.py`）；项目题对象是`docs/learning-records/L04-code-map.json`中的实际源码。不能把这里的目标答案反填成你的实际观察。

对应题目和执行步骤：[03-实验与排错.md](03-实验与排错.md)；定位规则：[05-过关卡与代码地图.md](05-过关卡与代码地图.md)。

## 闭卷回忆参考

### 1. 一次LLM调用一定是Agent吗？

不是，核心看是否能在受控循环中依据环境反馈选择行动。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 2. 没有tool_call是否等于成功？

只说明模型本轮没有提出工具；需检查任务验收。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 3. 为什么脚本模型有价值？

能稳定触发每条状态转移，方便检查编排错误。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

## 原教程独立练习

题目：把max_steps从3改为1，预测并验证会少哪一次调用；保留错误终止信息。

提示：“最多一轮”允许第一轮返回工具请求，但不能进行第二次模型调用；说明你的终止输出语义。

参考：步数上限控制模型请求，不能隐藏工具执行事实。达到上限时返回未完成、已有证据和原因。

## 近迁移参考与验收

题目：增加waiting_for_user状态。

参考：缺少必要条件时结束本轮但保留任务；不继续空转，不计作失败修复。

先写失败输入与预期，再实现最小变化；保留一次失败与修复后的输出。检查通过不代表覆盖所有情况，至少解释一个尚未处理的边界。

## 远迁移参考与验收

题目：把循环迁移到运维诊断：指标异常但根因未知。

参考：列出允许观察、禁止动作、退出条件和证据，不直接赋予重启生产服务能力。

参考组织方式：复用本课决策方法，逐项列出新业务的身份、数据、副作用和结果来源；明确哪些旧假设已不成立。不要只替换类名。允许有不同实现，但必须给出能让错误方案失败的验收。

## 自评与下一步

每题按本文件“只说术语0分、解释机制1分、给出具体分支与反例2分”的标准；远迁移按4项各1分：条件明确、方案有比较、验证可执行、限制诚实。至少3/4且无硬边界错误为本轮通过。这里的阈值是课程训练门槛，不是行业招聘标准。

- 只说术语：回到01相应知识点，重走一次案例。
- 机制懂但无法编码：补本课语言连接项，独立写一个最小函数。
- 能完成原题但换条件失败：再做一个近迁移变体，记录失效的假设。
- 能讲方案却没有证据：按03重做故障注入并保存实际输出。

AI可以在你提交答案后评审，但不能替你给“已掌握”盖章。D7/D21检查用于发现遗忘，再决定补练。


## 代码地图预测题：目标契约与反事实

**题目：** 工具失败后，第二次模型请求仍看不到失败结果，会发生什么？

**参考判断：** 模型重复基于旧上下文决策，可能再次调用相同工具。把有call_id的真实错误结果追加进下一轮，而不是仅在终端打印。还要独立配置步数/时间上限。

**反事实核对：** 脚本模型连续要求同一工具：检查预算减少且循环终止；把反馈删除后比较事件轨迹。

**远迁移方向：** 工单Agent查不到用户时该补信息还是继续调用？用同一个状态机解释。

评分：只说名词0分；能说机制1分；能预测具体分支且给出反例2分。还必须有独立修改和失败证据；这个分数不是就业预测。允许有不同实现，但不得跳过安全/事实边界。

## 从实验页移入的独立变体参考

先明确step是模型轮次还是工具次数，再按该定义断言；不能把二者混为一谈。达到宿主上限后不再执行新工具，记录budget_exhausted和已有轨迹。
