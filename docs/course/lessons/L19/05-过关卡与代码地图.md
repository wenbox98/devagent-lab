# L19｜过关卡与代码地图：先选择你正在看的代码

[课程目录](../../01-课程导航.md) · [本课目的与文件导航](00-本课目的与文件导航.md) · [前一课 R02](../R02/00-本课目的与文件导航.md) · [后一课 L20](../L20/00-本课目的与文件导航.md)

**本文件用途：** 定位阅读和预测的对象；没有生成项目代码时不强迫你引用不存在的函数。

## 1. 先选择阶段，不混写证据

| 你现在的状态 | 应打开的对象 | 允许得出的结论 |
| --- | --- | --- |
| 还没生成项目 | [00-example.py](00-example.py)；改动在`.local/concepts/L19/00-example.py` | 只说明概念例子真实行为；项目目标用“应当” |
| 已让Codex实现 | `docs/learning-records/L19-code-map.json`列出的实际文件 | 沿实际源码、日志和测试解释，不默认与教材建议名称相同 |
| 找不到函数或结构不同 | [02-交给AI.md](02-交给AI.md) + 当前源码，选择ANALYZE_ONLY | 让Codex找职责与原文锚点；未找到明确missing，不硬猜 |

## 2. 概念例子具体从哪里读

完整基准路径：`docs/course/lessons/L19/00-example.py`。具体看：allowed状态迁移表、can_move(old,new)及两个调用。

以下行号是**本包概念示例**的定位辅助，不是你的项目行号；副本修改后行号可能变化，以符号/原文为准。

| 已提供符号 | 基准起始行 |
| --- | --- |
| `can_move` | 6 |

**变化任务：** 追加can_move("running","succeeded")和can_move("succeeded","running")，只改输入，不放宽终态规则。

**观察目标：** 状态迁移是否允许；完整项目需查询持久记录和领取竞争。

运行方法与副本创建方式见[实验与排错](03-实验与排错.md)A阶段。你不需要先把项目代码全部生成。

## 3. 项目目标预测：明确这是“应该怎样”，不是“现在已经怎样”

**问题：HTTP超时后Java把Python任务写成failed，为什么可能产生双重事实？**

目标数据流：POST先落库 → 202+execution_id → worker条件领取 → GET状态/事件。

未实现时答“目标在哪个职责边界判断、应阻止或允许什么、需要什么证据”，不填函数位置。已实现时，用源码地图查到真正入口与分支，再回答“当前代码实际怎样”。结果不符合目标正是要排查的偏差，不能把答案里的目标行为写成观察到的事实。

## 4. 让Codex找五类位置，而不是找五个预设文件名

| 角色 | 必须从实际代码回答的问题 | 证据要求 |
| --- | --- | --- |
| 入口 | 本课输入从哪个函数、HTTP接口、测试或命令进入？ | 实际路径+符号/配置块 |
| 输入边界 | 哪个条件首先决定继续还是拒绝？ | 原文锚点+具体输入类型/值 |
| 核心决策 | 怎样推进本课目标：提交接口与持久任务？ | 关键分支/字段前后值 |
| 下游/状态 | 调用了什么或修改了什么，次数与身份是什么？ | 调用参数/次数、状态、文件或记录 |
| 测试 | 哪条断言会让错误实现失败？ | 具体测试方法、实际收集数与输出 |

有些角色由同一文件承担，有些课没有模型调用：不要把所有章节都强行解释为client调用数。Java类路径、SQL迁移脚本与评测文件都可以成为真实观察对象。

| 职责：应该看什么内容 | 新项目建议落点（不是已生成的文件） | 在当前仓库如何确认 |
| --- | --- | --- |
| 任务API | `devagent/service/api.py` | Codex先读取现有源码，给出实际文件、函数/类/配置块和原文锚点；没有就标“待生成” |
| 持久状态和版本 | `devagent/service/store.py` | Codex先读取现有源码，给出实际文件、函数/类/配置块和原文锚点；没有就标“待生成” |
| 领取任务worker | `devagent/service/worker.py` | Codex先读取现有源码，给出实际文件、函数/类/配置块和原文锚点；没有就标“待生成” |

## 5. 可以直接交给Codex的定位请求

```text
请按同目录02-交给AI.md的ANALYZE_ONLY模式分析L19，不要修改代码。
我想弄清本课意图：浏览器连接断开以后，任务还在哪里，谁拥有它的真实状态？
请读取当前源码，按入口、输入边界、核心决策、下游/状态、测试五种职责定位。
每个位置给实际路径、真实符号或配置块、原文锚点；不存在则明确missing。
建议路径不是实际路径，概念示例不是应用实现。
最后用一个正常例子和一个失败例子解释当前行为与目标合同的差距。
不要提前提供独立练习答案。
```

在IDE/Codex中仅附本课02也能完成同样分析；这里的请求只是便于复述，不需要再拼接本文件。

## 6. 过关证据保存在哪

个人记录：`docs/learning-records/L19.md`。源码地图：`docs/learning-records/L19-code-map.json`，只有实施阶段由Codex写入真实映射；只读分析阶段在回复中展示，不写文件。

按“输入/状态 → 首个相关分支 → 下游或状态变化 → 实际证据”作答。数据导出服务的请求生命周期与后台导出生命周期如何分离？

源码定位检查只验证文件、锚点与哈希存在，不证明业务正确。仍需[实验案例](03-实验与排错.md)与独立修改。预测/迁移的参考在[04-复习与迁移答案.md](04-复习与迁移答案.md)。
