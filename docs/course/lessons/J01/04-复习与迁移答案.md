# J01｜复习与迁移答案：先尝试，再打开

[课程目录](../../01-课程导航.md) · [上一课 L20](../L20/01-教程.md) · [下一课 J02](../J02/01-教程.md)

这份给学习者核对，**不需要交给代码AI生成基础实现**。参考解法不是唯一解，关键是约束、证据与边界。

## 先确认你核对的是哪个对象

本文件的“应该”是目标合同，不是对未生成项目的实测报告。概念题对象是`docs/course/lessons/J01/Demo.java`（练习改动在`.local/concepts/J01/Demo.java`）；项目题对象是`docs/learning-records/J01-code-map.json`中的实际源码。不能把这里的目标答案反填成你的实际观察。

对应题目和执行步骤：[03-实验与排错.md](03-实验与排错.md)；定位规则：[05-过关卡与代码地图.md](05-过关卡与代码地图.md)。

## 闭卷回忆参考

### 1. 为什么@Transactional不够？

事务控制本地数据库，不控制远程Python副作用。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 2. 只查重再插入为什么会重复？

并发窗口两请求均查不到；用唯一约束原子裁决。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 3. Outbox是否无需幂等？

仍需，投递后状态未保存可能重发。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

## 原教程独立练习

题目：同样幂等键由两个不同用户使用，验证身份范围既不互相阻塞也不越权返回结果。

提示：key通常需要作用域，不能让全局key碰撞泄露任务。

参考：唯一约束包含可信owner，查询也按owner过滤；execution_id对外不可作为权限凭据。

## 近迁移参考与验收

题目：增加并发50次同key提交实验。

参考：断言本地业务记录1条、稳定远端ID1个；HTTP响应数不是执行次数。

先写失败输入与预期，再实现最小变化；保留一次失败与修复后的输出。检查通过不代表覆盖所有情况，至少解释一个尚未处理的边界。

## 远迁移参考与验收

题目：迁移到支付后发货。

参考：支付与发货用稳定业务标识和状态对账，分别处理重复与未知，不能用本地回滚声称远端已撤销。

参考组织方式：复用本课决策方法，逐项列出新业务的身份、数据、副作用和结果来源；明确哪些旧假设已不成立。不要只替换类名。允许有不同实现，但必须给出能让错误方案失败的验收。

## 自评与下一步

每题按本文件“只说术语0分、解释机制1分、给出具体分支与反例2分”的标准；远迁移按4项各1分：条件明确、方案有比较、验证可执行、限制诚实。至少3/4且无硬边界错误为本轮通过。这里的阈值是课程训练门槛，不是行业招聘标准。

- 只说术语：回到01相应知识点，重走一次案例。
- 机制懂但无法编码：补本课语言连接项，独立写一个最小函数。
- 能完成原题但换条件失败：再做一个近迁移变体，记录失效的假设。
- 能讲方案却没有证据：按03重做故障注入并保存实际输出。

AI可以在你提交答案后评审，但不能替你给“已掌握”盖章。D7/D21检查用于发现遗忘，再决定补练。


## 代码地图预测题：目标契约与反事实

**题目：** Java事务回滚了，但Python已开始运行，任务是否随之撤销？

**参考判断：** 不会自动撤销。事务只覆盖本地数据库边界。稳定执行ID、幂等提交和对账恢复处理远程未知；同键不同payload返回冲突。

**反事实核对：** 模拟Python接受后响应丢失，再重试提交；按execution_id确认没有第二个逻辑任务。

**远迁移方向：** 把Python换成物流发货API，哪些相同，哪些业务补偿不能由技术幂等替代？

评分：只说名词0分；能说机制1分；能预测具体分支且给出反例2分。还必须有独立修改和失败证据；这个分数不是就业预测。允许有不同实现，但不得跳过安全/事实边界。

## 从实验页移入的独立变体参考

本地唯一约束与远端幂等/查询共同验证，不能只看Java行数。相同key不同body返回冲突；日志不能暴露认证信息。
