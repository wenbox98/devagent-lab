# L05｜复习与迁移答案：先尝试，再打开

> v5阅读说明：先保留你在03/05中的首次判断再核对。本文件保留原课参考思路，不代表当前主项目已全部实现，也不代表学习者已掌握。

## v5核对时的三条约束

本课基础方案以[新版教程](01-教程.md)第1/3节和[02合同](02-交给AI.md)为准；原参考中的建议文件/符号需要映射真实代码。不要以修改参考标准迎合错误实现。

验证必须说明输入、第一处关键判断、实际副作用和结果。模拟、框架离线、真实服务、跨进程持久性和个人能力分开报告。

如果参考中的扩展条件超出现有代码，写待实现/未验证，不能把目标当当前事实。L00—L02具体差距见[当前基线](../../19-当前代码基线与学习接续.md)。

---

## 原课参考内容（保留作对照）

[课程目录](../../01-课程导航.md) · [上一课 L04](../L04/01-教程.md) · [下一课 L06](../L06/01-教程.md)

这份给学习者核对，**不需要交给代码AI生成基础实现**。参考解法不是唯一解，关键是约束、证据与边界。

## 先确认你核对的是哪个对象

本文件的“应该”是目标合同，不是对未生成项目的实测报告。概念题对象是`docs/course/lessons/L05/00-example.py`（练习改动在`.local/concepts/L05/00-example.py`）；项目题对象是`docs/learning-records/L05-code-map.json`中的实际源码。不能把这里的目标答案反填成你的实际观察。

对应题目和执行步骤：[03-实验与排错.md](03-实验与排错.md)；定位规则：[05-过关卡与代码地图.md](05-过关卡与代码地图.md)。

## 闭卷回忆参考

### 1. 命中一个文件为何不够？

还需核对语义、版本、相关调用和约束。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 2. 增大top_k一定更好吗？

不一定，噪声与上下文成本也增加。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 3. 没搜到就不存在吗？

未必，查询、索引范围、分页或权限都可能影响召回。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

## 原教程独立练习

题目：把任务换一种说法但不变目标，检查是否仍定位到同一相关代码。

提示：不要在新任务中直接泄漏正确文件名，否则测不到理解任务与检索的能力。

参考：保留任务意图、换措辞，并使用同一仓库版本；对结果差异看工具轨迹，别只凭最终回答猜原因。

## 近迁移参考与验收

题目：加入第三个同名函数，保持原问题。

参考：定位目标仍正确，证据说明排除另外两个的依据。

先写失败输入与预期，再实现最小变化；保留一次失败与修复后的输出。检查通过不代表覆盖所有情况，至少解释一个尚未处理的边界。

## 远迁移参考与验收

题目：把方法用于排查配置中心开关影响范围。

参考：检索配置定义、读取位置、调用路径，分别记录版本；不能只凭配置名下结论。

参考组织方式：复用本课决策方法，逐项列出新业务的身份、数据、副作用和结果来源；明确哪些旧假设已不成立。不要只替换类名。允许有不同实现，但必须给出能让错误方案失败的验收。

## 自评与下一步

每题按本文件“只说术语0分、解释机制1分、给出具体分支与反例2分”的标准；远迁移按4项各1分：条件明确、方案有比较、验证可执行、限制诚实。至少3/4且无硬边界错误为本轮通过。这里的阈值是课程训练门槛，不是行业招聘标准。

- 只说术语：回到01相应知识点，重走一次案例。
- 机制懂但无法编码：补本课语言连接项，独立写一个最小函数。
- 能完成原题但换条件失败：再做一个近迁移变体，记录失效的假设。
- 能讲方案却没有证据：按03重做故障注入并保存实际输出。

AI可以在你提交答案后评审，但不能替你给“已掌握”盖章。D7/D21检查用于发现遗忘，再决定补练。


## 代码地图预测题：目标契约与反事实

**题目：** 两个文件都有page函数，第一条搜索命中足够授权修改吗？

**参考判断：** 不够。先用调用关系、目录职责、测试和任务约束消歧；命中只负责候选定位，精读与版本核对负责确认。保留路径和行范围使结论可复查。

**反事实核对：** 加入reporting.py同名函数；只改搜索排序，不改测试。观察Agent是否错误地把排名当事实。

**远迁移方向：** 知识库两篇同名政策，一篇过期：排序与生效版本的职责怎么分？

评分：只说名词0分；能说机制1分；能预测具体分支且给出反例2分。还必须有独立修改和失败证据；这个分数不是就业预测。允许有不同实现，但不得跳过安全/事实边界。

## 从实验页移入的独立变体参考

精确文本、语义描述、无答案应分组；同义描述召回下降不自动证明需要向量库，也可先检查可搜索字段和查询改写。不存在的目标应允许无结果，不用最大分值强选。
