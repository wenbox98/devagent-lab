# L17｜实验与排错：一个输入、一个假设、一条证据

[课程总览](../../01-课程导航.md) · [本课入口](00-本课目的与文件导航.md) · [上一课 L16](../L16/00-本课目的与文件导航.md) · [下一课 L18](../L18/00-本课目的与文件导航.md)

## A. 先选对象，不等于要全部重做

未生成主项目时先跑概念例子；主项目命令必须在02实施完成后再执行。

执行目录是学习项目根目录（能看到devagent/与docs/course/）。准备原教材例子的副本：

```bash
python docs/course/tools/prepare_example.py L17 --project .
```

只改`.local/concepts/L17/00-example.py`，不改教材基准。具体观察：strategies字典、sum(calls)和len(calls)。

先预测，再改变条件：把small的调用次数从5改为2；保持单次教学成本0.02，比较整任务合计。

验证范围：总额、次数和成功条件；单次便宜不保证整任务更便宜。。这不是当前应用已完成的证明。

### 概念副本怎么运行

```bash
python .local/concepts/L17/00-example.py
```

## B. 项目实现：先基线，再变更

把同目录02交给Codex，已有实现用REVIEW_ONLY，缺实现且明确授权时用IMPLEMENT。先读取相关旧测试、实际源码与Git状态，不能从模块不存在推断需要安装同名第三方包。

实施后按02列出的案例逐个跑。每个案例运行前写：输入、预期首个分支、调用/副作用、预期结果；运行后另写observed，不把预测复制成观测。

### 必需案例与实际要证明的行为

| 案例名 | 怎么触发 | 必须实际验证的结果 |
| --- | --- | --- |
| cost-sum | 注入不同轮数的已知usage记录 | 按整任务累计，不只比较单次价格 |
| unknown-usage | 某次调用没有usage | 报告费用不完整，不当0 |
| bottleneck | 受控工具慢、模型快 | 报告主要耗时在工具，优化建议与事实一致 |
| strategy-compare | 同任务、同仓库比较两策略 | 列质量与成本，不预设改动有益 |

## C. 只做一个独立变体

选一次你能复现的重复取证，提出缓存键并设计版本改变的失败用例；不先实现通用缓存平台。

记录第一次判断，不先打开04。先写最小失败复现；定位首个不同分支后再改对应职责。不要同时修改模型、提示词、依赖与测试期望。

若卡住，给导师：实际输入、执行命令、预期、实际错误、两个可能原因、已排除哪个。请求定位提示，而不是让AI重写项目。

## D. 怎样收尾

先运行新增测试（用实际类名/方法），再当课与全量回归：

```bash
python -m unittest discover -s tests -p "test_l17.py" -v
python -m unittest discover -s tests -v
```

若项目选择pytest，按实际测试入口替换，不同时发明另一套测试组织。发现数必须大于0。

检查Git diff只改变本课职责与测试；只记录实际通过/失败/skip/blocked，不把平台不支持写成通过。结果写入`docs/learning-records/L17.md`，数据源与个人理解分开。

用[05过关卡](05-过关卡与代码地图.md)复述一个正常与失败路径，再查[04参考](04-复习与迁移答案.md)。D1/D3/D7/D21只是可选复测安排，以实际完成日为起点，不自动产生提醒或成绩。

## 本次只做这一步

先看任务账本中最耗时的阶段，再决定优化对象；不要从某个模型单价开始。
