# F02｜复习与迁移答案

先完成03和05中的作答；这不是项目的完整参考实现。


## 先确认你核对的是哪个对象

本文件的“应该”是目标合同，不是对未生成项目的实测报告。概念题对象是`docs/course/lessons/F02/00-example.py`（练习改动在`.local/concepts/F02/00-example.py`）；项目题对象是`docs/learning-records/F02-code-map.json`中的实际源码。不能把这里的目标答案反填成你的实际观察。

对应题目和执行步骤：[03-实验与排错.md](03-实验与排错.md)；定位规则：[05-过关卡与代码地图.md](05-过关卡与代码地图.md)。

## 代码地图预测题：目标契约与反事实

**题目：** 换成create_agent后路径检查应该删掉吗？schema为何不能替代它？

**参考判断：** 不能删。框架负责消息和编排，领域执行器负责权限、路径、版本与效果。相同正常/拒绝案例应在native和langchain路径下满足相同业务不变量。

**反事实核对：** 固定脚本模型和输入，只换编排；核对ToolMessage的tool_call_id与工具实际执行次数。

**远迁移方向：** 把一个HTTP框架换成另一个时，为什么业务授权不应埋在控制器注解里而完全丢失？

评分：只说名词0分；能说机制1分；能预测具体分支且给出反例2分。还必须有独立修改和失败证据；这个分数不是就业预测。允许有不同实现，但不得跳过安全/事实边界。

## 从实验页移入的独立变体参考

添加包装与schema，不另写一套不安全文件读取。测试同时检查框架协议与领域结果，AST符号索引不是语义向量检索。

这是判断方向，不是完整实现。用自己的代码、失败复现和结果证明它，而不是复述答案。
