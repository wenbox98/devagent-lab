# L12｜复习与迁移答案：先尝试，再打开

[课程目录](../../01-课程导航.md) · [上一课 L11](../L11/01-教程.md) · [下一课 F01](../F01/01-教程.md)

这份给学习者核对，**不需要交给代码AI生成基础实现**。参考解法不是唯一解，关键是约束、证据与边界。

## 先确认你核对的是哪个对象

本文件的“应该”是目标合同，不是对未生成项目的实测报告。概念题对象是`docs/course/lessons/L12/00-example.py`（练习改动在`.local/concepts/L12/00-example.py`）；项目题对象是`docs/learning-records/L12-code-map.json`中的实际源码。不能把这里的目标答案反填成你的实际观察。

对应题目和执行步骤：[03-实验与排错.md](03-实验与排错.md)；定位规则：[05-过关卡与代码地图.md](05-过关卡与代码地图.md)。

## 闭卷回忆参考

### 1. 模型总结与测试冲突信谁？

先核对当前版本的真实工具证据。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 2. 通过测试等于零缺陷吗？

不，结论受测试与环境覆盖限制。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

### 3. 硬约束为何不加权？

违规不能被其他优点抵消，应先门控。

追问：请在自己的代码/trace中指认一个相应位置；只背这句话不得满分。

## 原教程独立练习

题目：写一个能骗过单个样例的错误实现，再增加一个测试将其识别。

提示：先问当前测试没有约束哪个输入维度。

参考：例如对固定列表返回首两项的硬编码会被不同列表或第二页测试识别；测试应针对错误假设，而非复制正确实现。

## 近迁移参考与验收

题目：加入测试报告版本校验。

参考：运行后再改一字节，旧报告必须失效；未改时正常通过。

先写失败输入与预期，再实现最小变化；保留一次失败与修复后的输出。检查通过不代表覆盖所有情况，至少解释一个尚未处理的边界。

## 远迁移参考与验收

题目：迁移到报表Agent：生成了文件但数值不对。

参考：检查数据快照、计算规则、行数合计与权限，生成成功不等于报表正确。

参考组织方式：复用本课决策方法，逐项列出新业务的身份、数据、副作用和结果来源；明确哪些旧假设已不成立。不要只替换类名。允许有不同实现，但必须给出能让错误方案失败的验收。

## 自评与下一步

每题按本文件“只说术语0分、解释机制1分、给出具体分支与反例2分”的标准；远迁移按4项各1分：条件明确、方案有比较、验证可执行、限制诚实。至少3/4且无硬边界错误为本轮通过。这里的阈值是课程训练门槛，不是行业招聘标准。

- 只说术语：回到01相应知识点，重走一次案例。
- 机制懂但无法编码：补本课语言连接项，独立写一个最小函数。
- 能完成原题但换条件失败：再做一个近迁移变体，记录失效的假设。
- 能讲方案却没有证据：按03重做故障注入并保存实际输出。

AI可以在你提交答案后评审，但不能替你给“已掌握”盖章。D7/D21检查用于发现遗忘，再决定补练。


## 代码地图预测题：目标契约与反事实

**题目：** 模型说成功、公共测试通过，但它修改了验收文件，能毕业吗？

**参考判断：** 不能。评测标准不能由被评对象一起改；检查参考测试hash和允许修改范围。报告还应绑定被测快照，防止旧测试报告验证新代码。

**反事实核对：** 使用随包grade_fixture.py对原始bug和正确修复分别判定；再篡改自述，结果应不受影响。

**远迁移方向：** SQL生成Agent怎样防止用删除数据“修复”失败校验？先定义不可改变的事实和允许操作。

评分：只说名词0分；能说机制1分；能预测具体分支且给出反例2分。还必须有独立修改和失败证据；这个分数不是就业预测。允许有不同实现，但不得跳过安全/事实边界。

## 从实验页移入的独立变体参考

前三份均不能成功，第四份才满足当前契约。验证器从真实报告和diff读取字段，不能接受Agent自己填写scope_ok或tests_run。
