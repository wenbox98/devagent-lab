# F04｜复习与迁移答案

先完成03和05中的作答；这不是项目的完整参考实现。


## 先确认你核对的是哪个对象

本文件的“应该”是目标合同，不是对未生成项目的实测报告。概念题对象是`docs/course/lessons/F04/00-example.py`（练习改动在`.local/concepts/F04/00-example.py`）；项目题对象是`docs/learning-records/F04-code-map.json`中的实际源码。不能把这里的目标答案反填成你的实际观察。

对应题目和执行步骤：[03-实验与排错.md](03-实验与排错.md)；定位规则：[05-过关卡与代码地图.md](05-过关卡与代码地图.md)。

## 代码地图预测题：目标契约与反事实

**题目：** state.evidence=[a]，节点返回[a,b]，append reducer后是什么？正确delta是什么？

**参考判断：** 得到[a,a,b]。在追加语义下应该只返回[b]；但不是所有字段都追加，remaining应覆盖。按字段定义更新语义和写入者，不把整个state原样返回。

**反事实核对：** 运行练习副本`.local/workshops/bug_lab/student.py`的`evidence_delta`题（`test_bug_lab.TestF04`）；再在真实图中检查每次节点后的快照。

**远迁移方向：** 事件溯源的增量事件和完整快照怎样避免重复应用？

评分：只说名词0分；能说机制1分；能预测具体分支且给出反例2分。还必须有独立修改和失败证据；这个分数不是就业预测。允许有不同实现，但不得跳过安全/事实边界。

## 从实验页移入的独立变体参考

需要清楚的合并合同或避免并发写同字段；列表追加可存独立证据，但不能自动解决同一状态值的冲突。不要用最后写入碰运气。

这是判断方向，不是完整实现。用自己的代码、失败复现和结果证明它，而不是复述答案。
