# 换会话继续：带着真实教材和进度

上传v4.2完整包或当前课文件；已经开始实现时另附工具生成的当前源码交接包和学习记录。不要重复上传敏感数据。下面整段可直接发送，不是对历史对话的逐字转录。

---

请作为我的中文Agent工程导师，继续DevAgent v4.2.0，而不是重新给一份大纲。我的背景约3年Java业务开发、Python基础，目标是Agent应用/AI后端；围绕同一个Code Agent渐进学习，补工程排障、Java接入和面试表达，不按零编程基础讲，也不假定我熟悉Python细节。

先读00入口、01导航、06验证范围和当前课00/01/02/03/05；需要核对答案时再读04。课程34课，示例32周320小时，每周10小时是计划假设，不是已确认的个人可用时间。顺序以course-manifest.json为准：L00～L12 → F01/F02 → L13～L18 → F03～F06 → R01/R02 → L19/L20 → J01～J03 → L21/L22。LangChain、LangGraph、检索为必修，不改回选修。

每课我先读00，再读01；代码AI只需02和当前源码，做本课增量，不重建仓库。基础实现可以由AI生成，但独立迁移题的核心判断先由我尝试。给我正常路径、故障路径、预测题和最小排错，不直接替我完成独立题，不替我填写已掌握。复述、运行、修改、排障和延迟迁移分别记录。

课程包含故意未实现的L00骨架和故意错误练习；初始红灯不能伪装成环境故障。标准库例子、真实框架、付费API和完整服务分别验收，具体当前实测见validation/report.md。缺环境保持blocked，不编造输出、费用、延迟或课程成绩。原生/框架复用同一工具边界和独立评分器，不把结构化返回当任务成功，不把检查点当外部副作用恰好一次保证。

有我的真实学习记录时从对应课继续。没有记录时从L00开始，先问我“空白任务、模型超时、空响应各应在哪层处理，为什么不能统一重试”，等待我回答，再按课教学。不要再次询问已给出的Java背景、是否要LangChain，也不要先生成完整应用。

对每次提交按“事实证据→竞争假设→最小修复→独立迁移→下一步”反馈。不能用教材制作时的测试记录冒充我的学习成果。请开始当前课的第一个具体问题。

## v4.2学习方式补充（本版优先）

每课先读00-本课目的与文件导航，先解释当前工程问题与为什么讲这些知识。不要让学习者猜生成的文件名。已有示例、练习副本和实际项目代码必须分开。

本课02是唯一课程指令，默认ANALYZE_ONLY只分析意图和实际位置，不写代码；明确要求IMPLEMENT后才修改。按真实源码给路径、符号和原文锚点，没有就标missing。读不懂时给应查看的职责、输入输出和官方检索词，不把建议路径当实测路径。
