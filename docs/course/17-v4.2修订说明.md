# v4.2.0修订说明｜完整教材，不是规则草案

日期：2026-09-18。输入为本对话已有完整v4 ZIP；没有读取你的实际学习仓库。此前两份v4.1仅为规则说明，不作为本版教材正文替代物。

## 实际修改范围

34课全部新增`00-本课目的与文件导航.md`，并改写原五份核心文件，共204份。218个知识段补了具体学习目的问题；00页也提供知识段与目的对照、官方检索词、前置与范围边界。

各课明确区分教材基准、个人概念副本、bug_lab错误练习、待生成应用和真实源码地图。应用职责建议不是实际路径断言；尤其Java包名、动态生成函数名不能预先编造。

34份02均改成自包含的ANALYZE_ONLY/IMPLEMENT/REVIEW_ONLY模式。只读分析不写源码或记录，不安装依赖或执行应用；明确实施后再做基础增量与实际验收。没有强制每课拆成固定四轮对话。

34份03明确运行目录、修改对象、观察字段和应用入口前置。独立题的折叠答案移至对应04。34份05不再强制未生成代码的学习者引用虚构函数位置；已有例子则直接给真实符号或变量和基准行号。

## 修复的具体误导

L00的整数42明确传给概念副本的handle；原始代码会先在strip处抛AttributeError，客户端调用0次，这不等于已返回规范invalid_input。应用run_task的目标契约、起始骨架NotImplementedError与概念行为分别说明。

L00的bad_response是模拟返回，不再写成HTTP已经成功。5000字符上限是基础题参数，8字符是后续变体。F04的错误练习函数实际是evidence_delta，不是不存在的merge_evidence。L05中遗留的RAG课号F03已改为R01/R02；不把F02内存FIXTURE的src/page.py当成用户磁盘文件。

## 新增可用工具

`tools/prepare_example.py`：按课复制概念例子到个人`.local/concepts/课号/`，拒绝覆盖已有练习。

`tools/probe_l00.py`：向真实handle注入JSON类型的输入，记录当前结果、异常和客户端调用次数；不把观察结果修饰成目标答案。

`tools/code_map.py`：只读列真实文件和Python符号；检查Codex地图的路径、符号、锚点和文件哈希。通过仅代表定位引用一致，不代表业务正确。

保留原骨架、31个Python概念例子、2个Java概念例子、SQL、8个真实框架脚本、八类错误练习与公开分页验收器。原始概念及框架源码不因本轮导航改写而被悄悄替换。

## 版本与验证边界

课数34、课程顺序、32周示例及320小时预算不变；00导航等修订并入原课预算，不另外承诺学习时间。依赖清单沿用v4；没有把未安装的框架声称为通过，也未重新核验所有外部参考页面。

本次实测与未运行项见[验证报告](validation/report.md)。v4历史记录已放入`validation/history-v4/`，不会作为当前验证结论。逐文件改动哈希在`revision-audit.json`，输入归档哈希在`source-provenance.json`。

已有学习者只需备份并更新`docs/course/`，不覆盖应用源码、Git或个人练习。按当前课00和02复核真实缺口，不必让Codex从头重建项目。

## 针对整数42补足练习断言

`workshops/bug_lab/test_bug_lab.py::TestL00.test_integer_task_is_rejected_before_client`是本版新增的公开测试：要求整数输入返回invalid_input且client调用数为0。参考实现本来已有类型检查，本版没有替换学习者的故意错误版本；概念探针仍如实记录原始概念例子的AttributeError。
