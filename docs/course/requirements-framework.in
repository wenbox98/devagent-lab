# v4 candidate direct dependencies, NOT a tested lockfile.
# Verified release/documentation anchors; current build environment could not install.
# Install in an isolated venv, pip check, run framework examples, THEN record your environment.
langchain==1.4.1
langchain-core==1.6.3
langgraph==1.2.11
pydantic==2.13.5
numpy==2.3.5
langgraph-checkpoint-sqlite==3.1.1
