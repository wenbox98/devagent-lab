# L01 实施证据

日期：2026-09-19。Git 起点：`0b9078aca29823e48cf31032678e3c28ecb4e492`。

以下仅记录实际实现和运行证据，不表示学习者已经掌握或完成独立迁移。

- 保留 L00 默认 fake 客户端、错误码、调用次数、trace 顺序和 13 项测试。
- 增加环境配置读取、OpenAI 兼容 HTTP 适配器、供应商响应归一化和运行时 fake/real 切换。
- 配置缺失、认证、限流、供应商超时、响应解析和其他服务错误分别映射；不自动重试。
- usage 缺失保持 `None`；供应商明确返回 0 时保留 0。
- L01 10 项测试通过；全量 23 项测试通过。
- fake-regression、missing-config、provider-errors、usage-unknown 四个离线演示通过。
- real-once 因缺少四项 `DEVAGENT_*` 配置而 blocked，退出 2，`paid_api_called=false`。

待学习者填写：

- 我如何解释 `ModelRequest → provider adapter → API → ModelResponse`：
- 为什么 usage 缺失不能写成 0：
- 200 响应但正文结构变化时，应在哪层处理：
- 我独立完成的迁移题与证据：
