# L01 实施证据

日期：2026-09-19。Git 起点：`0b9078aca29823e48cf31032678e3c28ecb4e492`。

以下仅记录实际实现和运行证据，不表示学习者已经掌握或完成独立迁移。

- 保留 L00 默认 fake 客户端、错误码、调用次数、trace 顺序和 13 项测试。
- 增加环境配置读取、OpenAI 兼容 HTTP 适配器、供应商响应归一化和运行时 fake/real 切换。
- 配置缺失、认证、限流、供应商超时、响应解析和其他服务错误分别映射；不自动重试。
- usage 缺失保持 `None`；供应商明确返回 0 时保留 0。
- L01 10 项测试通过；全量 23 项测试通过。
- fake-regression、missing-config、provider-errors、usage-unknown 四个离线演示通过。
- real-once 因缺少四项 `DEVAGENT_*` 配置而 blocked，退出 2，`paid_api_called=false`。

待学习者填写：

- 我如何解释 `ModelRequest → provider adapter → API → ModelResponse`：
- 为什么 usage 缺失不能写成 0：
- 200 响应但正文结构变化时，应在哪层处理：
- 我独立完成的迁移题与证据：

## 2026-09-20 响应包装兼容练习收尾

- 学习者提供了独立练习的修改意图和 `unwrap_response` 思路。本次检查起点为 `13610ecaa1bfcbc7397a194f1f64e41741dc055f`，工作区干净，磁盘上尚无该实现或新增测试；本轮按所述意图补全，不据此代写“已掌握”。
- adapter 支持直接响应和单层 `response` 包装；包装值不是 Mapping 时立即抛出 `ProviderResponseError`。
- 新增离线测试通过 ScriptedTransport 和 fixture_config 验证 answer、returned、输入用量 4、输出用量 1；另测非 Mapping 包装错误。app 层无需修改。
- L00/L01 CLI subprocess 明确使用 UTF-8 输出和解码，仅修复测试环境稳定性，保留原断言。
- 新增独立测试 1 项通过；L01 12 项通过；全量 25 项通过（L00 13 项），无 skip。首次独立测试因沙箱临时目录清理权限失败，正常权限重跑通过。
- 当前四项真实供应商配置仍缺失，real-once 保持 blocked；未发送真实 API 请求。
- 小型格式变化限制在 adapter；独立协议、认证、错误语义或 tool_calls 出现明显差异时再考虑独立 adapter，本次未做架构扩展。
