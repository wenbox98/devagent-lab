# L02 实施证据

日期：2026-09-20。起点 HEAD：`ebe195f3e2e5468f22838cf8b37d956f3d55e364`，master，工作区干净。

- 新增 `devagent/tools/files.py`：可信 root 规范化、目标 canonical path、工作区归属、可见路径和普通文本文件策略、UTF-8 解码、有界字节读取、1-based 包含尾行的范围。
- root 和 max_bytes 由程序调用者提供，demo 未开放 root 参数，也未新增模型工具调度。将来接入模型时必须由宿主固定 root，不能直接绑定模型提供的 root。
- 默认 64 KiB；先检查文件大小，再最多读取 max_bytes+1 字节以检测增长，超过预算明确拒绝。不会先无界读取再截断。行范围只作用于预算内文件。
- 返回 path/start_line/end_line/content/truncated/next_start_line。范围提前结束时标记剩余行；空文件或超出 EOF 返回空内容、end_line=start_line-1、无续读位置。未增加独立练习的最大行数限制。
- 复用已有 tiny_repo，保留 pagination.py 的 start=page_no*size 和 reporting.py 的同名 page。L00/L01 源码、旧测试和教材未修改。
- L02：收集 17 项，16 通过、1 skip、0 失败；全量：收集 42 项，41 通过、1 skip、0 失败。L00 的 13 项和 L01 的 12 项均通过。
- read-lines、escape、large-file 演示真实验证通过，退出 0。symlink 创建遇到 Windows WinError 1314，演示 blocked、verified=false、退出 2；真实 symlink 防护尚未在本环境验证，不计为通过。
- 测试观测文件在 stat 后增长时实际 read 参数为预算+1；原本已超限文件不执行 open。篡改 demo 的读取结果会使 verified=false。
- 初次测试发现 Windows 测试数据写入时转换换行；改为写明确 LF 字节，并增加 CRLF 原样读取测试后通过。

当前限制：resolve/归属检查不是完整沙箱，未解决检查与打开间路径替换竞态、硬链接别名；列表未设置总文件数/耗时预算；扩展名与 UTF-8 策略不识别代码内秘密。适用于受控、无恶意并发修改的教学工作区。

待学习者填写：路径边界解释、最大行数独立练习和个人理解。本记录仅陈述实现与验证证据，不代表“已掌握”。

## 2026-09-21 最大返回行数独立练习

- 起点 HEAD：`bfe236f2fb80ad8b5c5af21cad8a4a07cdedb137`。检查时 files.py 已有学习者本人设计并修改的 DEFAULT_MAX_LINES=50、max_lines 参数及校验、返回切片、list_files 的 stat 大小统计。
- 保留学习者原始逻辑。max_lines 限制实际返回行数；truncated 仍表示实际末行之后文件还有内容，next_start_line 指向下一行。stat 使用授权结果的工作区相对路径，统计完整文件字节数；原有并发路径替换风险未解决。
- Codex 只修正源码 docstring，新增 4 项行为测试并更新学习记录/代码地图；未读取参考答案、未修改教材。
- 100 行文件的 1~100 和 1~50 请求均返回至 50、truncated=True、next=51；80~150 返回 21 行至 100、truncated=False、next=None。非法 0/-1/True/2.5 均抛 ValueError。
- 测试还验证默认/自定义行数、续读和超过 50 行的 UTF-8/CRLF 文件完整字节大小。字节预算与路径拒绝等原有测试保留。
- 本轮 L02 收集 21 项：20 passed、0 failed、1 skipped；全量收集 46 项：45 passed、0 failed、1 skipped。L00/L01 共 25 项全部通过。
- symlink 仍因 Windows WinError 1314 缺少创建权限而 skip，不计为通过。本节记录独立实现与复核证据，不代写个人掌握结论。
